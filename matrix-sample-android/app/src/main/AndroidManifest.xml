<manifest xmlns:android="http://schemas.android.com/apk/res/android"

          package="sample.tencent.matrix">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>

    <application
        android:name=".MatrixApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">

        </activity>

        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".io.TestIOActivity"></activity>
        <activity android:name=".resource.TestLeakActivity"></activity>
        <activity android:name=".trace.TestTraceMainActivity"></activity>
        <activity android:name=".trace.TestEnterActivity"></activity>
        <activity android:name=".trace.TestFpsActivity"></activity>
        <activity android:name=".trace.TestStartUpActivity"></activity>
        <activity android:name=".sqlitelint.TestSQLiteLintActivity"></activity>
        <activity android:name=".battery.TestBatteryActivity"></activity>
        <activity android:name=".device.TestDeviceActivity"></activity>
        <activity
            android:name=".trace.TestOtherProcessActivity"
            android:process=".test"></activity>
        <activity android:name=".device.TestDeviceActivity"></activity>

        <receiver android:name=".battery.TestReceiver$TestBroadcastReceiver">
            <intent-filter>
                <action android:name="com.tencent.matrix.AlarmTest"></action>
            </intent-filter>
        </receiver>
        <receiver
            android:name=".trace.StartUpBroadcastReceiver"
            android:process=".test"/>

        <service
            android:name=".trace.StartUpService"
            android:process=".test"/>
        <provider
            android:name=".trace.StartUpContentProvider"
            android:authorities="sample.tencent.matrix.trace.StartUpContentProvider"
            android:process=".test"/>

        <receiver android:name=".battery.TestReceiver$TestBroadcastReceiver2"/>
        <receiver android:name=".battery.TestReceiver$TestBroadcastReceiver3"/>
        <receiver android:name=".battery.TestReceiver$TestBroadcastReceiver4"/>

    </application>

</manifest>
