version: 2
jobs:
  test:
    working_directory: ~/repo/matrix/matrix/matrix-android
    docker:
      - image: gengjiawen/android-ndk:android-ndk-r16b
    steps:
      - checkout
      - run:
          name: Build
          command: |
            ./gradlew assemble
#      - run:
#          name: Check
#          command: |
#            ./gradlew checkstyle

workflows:
  version: 2
  build_and_test:
    jobs:
      - test
